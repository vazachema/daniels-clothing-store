info:
  name: Get All
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/products? {{baseUrl}}/products"
  params:
    - name: page
      value: "1"
      type: query
      disabled: true
    - name: limit
      value: "10"
      type: query
      disabled: true
    - name: " {{baseUrl}}/products"
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
