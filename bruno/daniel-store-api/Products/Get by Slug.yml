info:
  name: Get by Slug
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/products/{{slug}}"
  auth: inherit

runtime:
  variables:
    - name: slug
      value: camiseta-blanca-basica

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
