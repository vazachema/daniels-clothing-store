info:
  name: Get Cart
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/cart/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
