info:
  name: Cancel Order
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/orders/pending"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
